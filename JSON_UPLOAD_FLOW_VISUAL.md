# 🎨 Visual Flow: Your JSON File Upload

## 📁 Your File Structure

```
data.json
└── {
    ├── users: [3 objects]
    │   ├── Object 1: {name: {first, last}, address: {...}, ...}
    │   ├── Object 2: {name: {first, last}, address: {...}, ...}
    │   └── Object 3: {name: {first, last}, address: {...}, ...}
    │
    ├── products: [3 objects]
    │   ├── Object 1: {sku, name, dimensions: {...}, ...}
    │   ├── Object 2: {sku, name, specs: {...}, ...}
    │   └── Object 3: {sku, name, specs: {...}, ...}
    │
    └── orders: [3 objects]
        ├── Object 1: {items: [...], shippingAddress: {...}, ...}
        ├── Object 2: {items: [...], shippingAddress: {...}, ...}
        └── Object 3: {items: [...], shippingAddress: {...}, ...}
    }
```

---

## 🔄 Processing Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│ 1. FILE UPLOAD                                              │
│    POST /api/ingest/upload                                  │
│    File: data.json                                          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. STAGING                                                   │
│    PostgreSQL: staging_files table                         │
│    - id: <uuid>                                             │
│    - originalFilename: "data.json"                          │
│    - filePath: "./uploads/<uuid>.json"                      │
│    - sha256: <hash>                                         │
│    - status: "pending"                                       │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. FILE TYPE DETECTION                                      │
│    Extension: .json                                         │
│    MIME: application/json                                   │
│    Category: json                                           │
│    → Routes to JSON Pipeline                                 │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. JSON PROCESSING                                          │
│    jsonPipelineService.processJson()                        │
│    - Reads file                                             │
│    - Parses JSON                                            │
│    - Returns: {data, analysis}                               │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. STRUCTURE ANALYSIS                                       │
│    analyzeJsonStructure()                                   │
│                                                              │
│    Detects:                                                  │
│    ✓ Type: "object" (not array)                             │
│    ✓ Fields: ["users", "products", "orders"]               │
│    ✓ Each field: type="array", nested=true                  │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. BACKEND DECISION                                         │
│    determineBackend()                                       │
│                                                              │
│    Checks: Has nested fields?                               │
│    → users: nested=true ✅                                  │
│    → products: nested=true ✅                               │
│    → orders: nested=true ✅                                 │
│                                                              │
│    Decision: MongoDB (NoSQL) ✅                             │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. SCHEMA GENERATION                                         │
│    - JSON Schema (for reference)                            │
│    - SQL DDL (generated but not used)                       │
│    - Field definitions                                       │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│ 8. DATA STORAGE                                             │
│    MongoDB: dataset_<uuid> collection                      │
│                                                              │
│    Document stored:                                         │
│    {                                                         │
│      users: [...],                                          │
│      products: [...],                                       │
│      orders: [...],                                         │
│      _datasetId: <uuid>,                                    │
│      _importedAt: <timestamp>                               │
│    }                                                         │
│                                                              │
│    Record count: 1 document                                 │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│ 9. CATALOG ENTRY                                            │
│    MongoDB: datasets collection                            │
│                                                              │
│    {                                                         │
│      datasetId: <uuid>,                                     │
│      originalName: "data.json",                            │
│      storage: "mongodb",                                    │
│      recordCount: 1,                                        │
│      schema: {...},                                         │
│      metadata: {...}                                        │
│    }                                                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Storage Map

```
┌─────────────────────────────────────────────────────────────┐
│ PostgreSQL (Docker)                                         │
│ ┌───────────────────────────────────────────────────────┐   │
│ │ staging_files table                                   │   │
│ │ ┌─────────────────────────────────────────────────┐   │   │
│ │ │ id | filename | sha256 | status | created_at    │   │   │
│ │ │ ...| data.json| <hash> | pending| 2025-11-15   │   │   │
│ │ └─────────────────────────────────────────────────┘   │   │
│ └───────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ MongoDB (Docker)                                            │
│ ┌───────────────────────────────────────────────────────┐   │
│ │ Collection: dataset_<uuid>                           │   │
│ │ ┌─────────────────────────────────────────────────┐   │   │
│ │ │ {                                                │   │   │
│ │ │   users: [3 user objects],                      │   │   │
│ │ │   products: [3 product objects],                 │   │   │
│ │ │   orders: [3 order objects],                    │   │   │
│ │ │   _datasetId: <uuid>,                           │   │   │
│ │ │   _importedAt: ISODate(...)                     │   │   │
│ │ │ }                                                │   │   │
│ │ └─────────────────────────────────────────────────┘   │   │
│ │                                                       │   │
│ │ Collection: datasets                                  │   │
│ │ ┌─────────────────────────────────────────────────┐   │   │
│ │ │ {                                                │   │   │
│ │ │   datasetId: <uuid>,                            │   │   │
│ │ │   originalName: "data.json",                   │   │   │
│ │ │   storage: "mongodb",                           │   │   │
│ │ │   recordCount: 1,                               │   │   │
│ │ │   schema: {...},                                │   │   │
│ │ │   metadata: {...}                              │   │   │
│ │ │ }                                                │   │   │
│ │ └─────────────────────────────────────────────────┘   │   │
│ └───────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Breakdown

### What Gets Stored:

| Component | Count | Location |
|-----------|-------|----------|
| **Main Document** | 1 | MongoDB: `dataset_<uuid>` |
| **Users** | 3 | Inside main document |
| **Products** | 3 | Inside main document |
| **Orders** | 3 | Inside main document |
| **Staging Record** | 1 | PostgreSQL: `staging_files` |
| **Catalog Entry** | 1 | MongoDB: `datasets` |

### Total Records:
- **MongoDB:** 1 document (contains everything)
- **PostgreSQL:** 1 staging record

---

## 🔍 How to Access Your Data

### Via MongoDB Shell:
```javascript
// Connect to MongoDB
docker exec -it smart_storage_mongodb mongosh -u admin -p admin --authenticationDatabase admin

// Use your database
use smart_storage

// Find your dataset
db.datasets.findOne({originalName: "data.json"})

// Get the data
db.getCollection('dataset_<uuid>').findOne()

// Get just users
db.getCollection('dataset_<uuid>').findOne({}, {users: 1})

// Get just products
db.getCollection('dataset_<uuid>').findOne({}, {products: 1})
```

### Via API:
```bash
# Get dataset info
GET /api/datasets

# Retrieve data
POST /api/retrieve
{
  "dataset": "<datasetName>",
  "entity": "dataset_<uuid>",
  "filter": {},
  "limit": 10
}
```

---

## ✅ Final Answer

**Database Used:** **MongoDB (Docker)**

**Why MongoDB?**
- Your JSON has nested arrays at the top level
- Each array contains complex nested objects
- MongoDB preserves the entire structure perfectly
- Better suited for hierarchical/nested data

**What Gets Stored:**
- **1 document** in MongoDB containing all users, products, and orders
- **1 staging record** in PostgreSQL (file metadata)
- **1 catalog entry** in MongoDB (dataset information)

**Structure Preserved:**
- ✅ All nested objects maintained
- ✅ Arrays stored as arrays
- ✅ Original structure intact

Your data is now queryable and accessible through the API! 🎉

